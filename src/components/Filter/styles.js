import { css } from 'styled-components';

const filterStyles = props => css`
  select {
    border: 1px solid gray;
    display: inline-block;
    border-radius: 4px;
    background: white;
    color: black;
  }
`;

export default filterStyles;
